# Multi Server Leaver Plugin - Quick Start

## Installation

1. Copy both files to your Vencord plugins folder:
   ```
   ~/.config/Vencord/plugins/simple-server-leave/
   ```
   Or on Windows:
   ```
   C:\Users\[USERNAME]\AppData\Roaming\Vencord\plugins\simple-server-leave\
   ```

2. Files needed:
   - `index.js` - The compiled plugin
   - `manifest.json` - Plugin metadata

3. Restart Discord

4. Go to Settings → Plugins → Multi Server Leaver and enable it

## Usage

- Click "📋 Select Mode" to start selecting servers
- Click servers to select/deselect them
- Click "🚪 Leave Selected" to leave all selected servers
- Confirm in the dialog

For full documentation, see: https://github.com/twisterboi123/simple-server-leave
